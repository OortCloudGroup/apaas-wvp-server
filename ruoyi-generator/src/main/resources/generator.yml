# 代码生成
gen:
  # 作者
  author: ruoyi
  # 默认生成包路径
  packageName: com.ruoyi.system
  # 自动去除表前缀，默认值false
  autoRemovePre: false
  # 表前缀(类名不会包含表前缀，多个用逗号分隔)
  tablePrefix: sys_
  # 是否允许覆盖已有文件
  allowOverwrite: false
